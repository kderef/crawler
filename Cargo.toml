[package]
name = "crawler"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[profile.dev.package."*"]
opt-level = 3

[profile.release]
codegen-units = 1
lto = true

[dependencies]
macroquad = {version = "0.4.14", features = ["audio"] }

[build-dependencies]
static_vcruntime = "2"
