[package]
name = "crawler"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[profile.dev]
package = {"*" = {opt-level = 3} }

[profile.release]
codegen-units = 1
lto = true

[dependencies]
raylib = "5.5.1"

[build-dependencies]
static_vcruntime = "2.0.0"
